<?php
/**
 * Custom post types for courses and lessons.
 *
 * @package OnlineLearning
 */

defined( 'ABSPATH' ) || exit;

/**
 * Registers `ols_course` and `ols_lesson` post types.
 */
final class OLS_Post_Types {
	/**
	 * Register post types (safe to call on `init` or activation).
	 */
	public static function register() {

	}
}
